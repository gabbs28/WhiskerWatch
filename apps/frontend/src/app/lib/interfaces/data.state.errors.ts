/**
 * Interface representing the structure of data state errors.
 *
 * This interface is used to define the properties related to errors within a data state.
 */
export interface DataStateErrors {
  message?: string;
}